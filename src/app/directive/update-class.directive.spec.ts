import { UpdateClassDirective } from './update-class.directive';

describe('UpdateClassDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateClassDirective();
    expect(directive).toBeTruthy();
  });
});
